---
import features from '../../config/features.json';
import '../../styles/Features.css';
---

<h1 class="text-center font-bold text-5xl mt-10 text-cyan-400">Características</h1>

<div class="grid md:grid-cols-3 gap-4 p-4">
    {
        features.map((feature) => (
            <div class="bg-slate-800 p-10 rounded-xl m-2 expand-on-hover bg-gradient-to-tr from-slate-900 to-slate-700">
                <h2 class="text-2xl font-bold my-4 text-cyan-200">{feature.title}</h2>
                <p class="text-cyan-100">{feature.description}</p>
            </div>
        ))
    }
</div>
